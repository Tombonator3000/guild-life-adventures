# Guild Life Adventures - Environment Variables
#
# Copy this file to .env.local and fill in your values.
# .env.local is gitignored and never committed.
#
# ─────────────────────────────────────────────────
# FIREBASE (required for "Search Online Games" and public lobby browser)
# ─────────────────────────────────────────────────
#
# Setup steps:
#   1. Go to https://console.firebase.google.com
#   2. Create a new project (or use an existing one)
#   3. Add a web app (click the </> icon)
#   4. Enable "Realtime Database" in the Firebase console
#      - Choose a region
#      - Start in "test mode" for development (remember to set rules for production!)
#   5. Copy the config values below from your Firebase app settings
#
# Recommended Firebase Realtime Database rules (set in Firebase console):
# {
#   "rules": {
#     "guild-life": {
#       "openGames": {
#         ".read": true,
#         "$roomCode": {
#           ".write": true,
#           ".validate": "newData.hasChildren(['roomCode','hostName','playerCount','maxPlayers','createdAt'])"
#         }
#       }
#     }
#   }
# }

VITE_FIREBASE_API_KEY=your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_DATABASE_URL=https://your-project-id-default-rtdb.firebaseio.com
VITE_FIREBASE_PROJECT_ID=your-project-id

# For GitHub Actions deployment, add these as repository secrets and reference them in your workflow.
