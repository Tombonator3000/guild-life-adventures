name: Apply phase 10 finance migration

on:
  push:
    branches:
      - agent/audit-phase10-finance
    paths:
      - phase10-finance-trigger.txt
      - .github/workflows/apply-phase10-finance.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/audit-phase10-finance
          fetch-depth: 1
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Apply migration
        run: node scripts/apply-phase10-finance.mjs
      - name: Commit migration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src docs
          git commit -m "Migrate finance services to host intent" || true
          git push origin HEAD:agent/audit-phase10-finance
