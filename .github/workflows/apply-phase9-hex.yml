name: Apply phase 9 hex migration

on:
  push:
    branches:
      - agent/audit-phase9-hex
    paths:
      - phase9-hex-trigger.txt
      - .github/workflows/apply-phase9-hex.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/audit-phase9-hex
          fetch-depth: 1
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Apply migration
        run: node scripts/apply-phase9-hex.mjs
      - name: Commit migration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src docs
          git commit -m "Migrate hex services to host intent" || true
          git push origin HEAD:agent/audit-phase9-hex
